{
  "name": "multi-agent-research",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node test/local.js",
    "bundle": "esbuild handler.js --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "deploy": "npm run bundle && cd infra && terraform apply"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@aws-sdk/client-lambda": "^3.1045.0",
    "@slack/web-api": "^7.15.2",
    "dotenv": "^16.0.0",
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
